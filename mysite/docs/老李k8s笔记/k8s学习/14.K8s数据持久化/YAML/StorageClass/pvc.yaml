apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc
  namespace: default
  labels:
    pv: nfs-pvc
spec:
  resources:
    requests:
      storage: 500Mi
  accessModes:
    - ReadOnlyMany
  storageClassName: nfs-csi